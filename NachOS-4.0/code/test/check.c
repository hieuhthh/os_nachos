#include "syscall.h"

int main()
{

    // char buffer[300];
    // int length = 5;
    // ReadString(buffer, length);
    // PrintString(buffer);
    // char buffer2[] = "adshakjshdakd\n";
    // PrintString(buffer2);
    Halt();
}